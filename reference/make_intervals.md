# create an intervals object

create an intervals object, assuming left-closed and right-open
intervals

## Usage

``` r
make_intervals(start, end)
```

## Arguments

- start:

  vector with start values, or 2-column matrix with start and end values
  in column 1 and 2, respectively

- end:

  vector with end values
